{% extends '::base.html.twig' %}

{% block body -%}
    <h1>back_end creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('back_end') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
