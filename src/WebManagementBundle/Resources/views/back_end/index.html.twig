{% extends '::base.html.twig' %}

{% block body -%}
    <h1>back_end list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id_back_end</th>
                <th>Tb_name</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('back_end_show', { 'id': entity.id_back_end }) }}">{{ entity.idbackend }}</a></td>
                <td>{{ entity.tbname }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('back_end_show', { 'id': entity.id_back_end }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('back_end_edit', { 'id': entity.id_back_end }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('back_end_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
